# Dispatch postmortem — A2 transport (2026-09-12)

Durable copy of the bus note `camera-a2-dispatch-postmortem`, because bus state
is per-checkout and never committed (build rule F2). What was delegated to
gpt-oss:20b, what it got wrong, why some parts went Claude-direct.

```
camera-a2-dispatch-postmortem â€” cli, 2026-09-12T18:04:01.282Z
  POSTMORTEM: what was delegated to gpt-oss:20b on the A2 transport slice, what it got wrong, and why some parts went Claude-direct (Austin asked this be on the board). DELEGATED: agent/api-server.mjs (generated, then integration-fixed) and the apiServer harness first cut. NOT DELEGATED: the spec (A2-TRANSPORT-SPEC.md - hub rule: spec is the orchestrator lane), segindex getByKey (3 lines - dispatch overhead exceeds the diff, no token saving), and the FINISHED harness (see below). GPT-ROUND-1 (server) errors, every one an invented API the model could not see from the spec alone: (1) namespace imports of nonexistent modules - 'import { apiQuery } from ../dist/apiQuery.mjs'; dist exports named functions from .js files; (2) 'instanceof ApiRefusal' against a TypeScript INTERFACE - refusals are values, detect with ok===false; (3) loadConfig called without await (async); (4) now() passed as a Date where contracts want a UTC string - coverageFromIndex would throw on every request; (5) resolveCameraUrl treated as throwing when it returns {kind:'unresolved'} - health unresolved would always read 0; (6) THE LATE ONE, caught at verify: fs/promises stat invoked with a callback - the promises API ignores the callback, the awaited Promise never settles, the handler hangs forever and fetch hangs with it. GPT-ROUND-2 (harness) errors: output TRUNCATED mid-line at 331 lines (the empty-response lesson - split dispatches, one function per call); invented an npm sqlite3 dependency and hand-rolled its own schema instead of the real openIndex API; invented _assert export 'awaitCheck' (does not exist; check returns the promise); wrong field names throughout (view.reason vs unresolvedReason, runs[].type vs kind, requested.start vs startUtc); fixture files at the wrong layout (store root instead of <root>/<cameraId>/<startMs>.mp4); asserted port 554 against its own spec fixture that says 8554; naive-instant check used a VALID Z instant; zero-filled fixture files, which would have made range-offset bugs pass vacuously (fixed: deterministic pseudo-random fill); no cleanup/close. WHY CLAUDE-DIRECT FINISHED THE HARNESS: two consecutive failures on the same file, and the hub's own rule that verification never delegates - a subtly-wrong harness is the exact class of wrongness that is invisible until it costs you. Also my own spec had three bugs the harness would have enshrined (window 10:00-10:10 yields 4 runs not 3, cam-1 port is 8554 not 554, gap fields are gapReason/gapSource) - re-derive expectations from the contracts, never from the spec prose alone. WHAT WORKS (do this on every dispatch): write the brief to a file; one function/file per dispatch; ALWAYS feed the fixed real file back as context for the next round; expect the orchestrator to integration-fix; run the harness yourself, never trust the generator's green.
```
